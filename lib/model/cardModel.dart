class CardModel {
  String image, tag, topic, subtitle1, subtitle2, companyName, companyTag;
  CardModel(this.image, this.tag, this.topic, this.subtitle1, this.subtitle2,
      this.companyName, this.companyTag);
}
